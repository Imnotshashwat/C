#include <stdio.h>

int main()
{
    int a[5];
    printf("enter 5 no:\n");
    for (int i = 0; i < 5; i++)
    {
        scanf("%d", &a[i]);
    }
    for (int i = 0; i < a[i]; i++)
    {
        printf("%d ", a[i]);
    } 
    // int b[5]={"ajay"};
    // printf("%s",a);

    return 0;
}